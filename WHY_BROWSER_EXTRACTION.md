# なぜClaude in Chromeでは取れるのに、CLIでは少し難しいのか

結論として、難しいというより **取得する場所が違う** のが理由です。

Claude in Chromeは、Chromeで実際に表示されたページを見ています。つまり、JavaScript実行後の画面、タブ切り替え後の内容、ログイン・Cookie・bot対策を通過した後のDOMを読めます。

一方、通常のPythonやcurlで `https://cafekai.jp/event` を直接取りに行く場合は、次の問題が起きることがあります。

- JavaScriptで後から表示されるイベントが、最初のHTMLに入っていない。
- 場所タブをクリックしないと、その場所のイベントがDOMに出てこない。
- サイト側のbot対策やWAFにより、ブラウザ以外のアクセスが拒否される。
- イベントタイトル、日付、場所のHTML構造が変更されると、抽出ロジックの調整が必要になる。

そのため、この用途では **ブラウザ内で抽出する方式** が現実的です。

## おすすめの実装方針

最短で実用化するなら、以下の順番がおすすめです。

1. Chromeで `https://cafekai.jp/event` を開く。
2. 場所タブを順番に表示する。
3. ブラウザ内JavaScriptで、表示中のイベント情報をCSVとしてコピーする。
4. `cafekai_campaign.py` にCSVを渡して、LINE用画像3枚を生成する。

完全自動化したい場合は、PlaywrightでChrome相当のブラウザを起動して、タブクリックとDOM抽出を自動化します。
